services:
  api:
    build: .
    ports:
      - "3000:3000"
    environment:
      JOBSHIELD_ARTIFACTS: data/artifacts.json
      PORT: 3000
      RATE_LIMIT_MAX: 120
      RATE_LIMIT_WINDOW_MS: 60000
    volumes:
      - ./data/artifacts.json:/app/data/artifacts.json:ro
