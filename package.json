{
  "name": "jobshield-ai",
  "version": "0.1.0",
  "private": true,
  "description": "Thai Occupation Mobility Network — explainable career path recommender",
  "type": "module",
  "workspaces": ["ts/*"],
  "scripts": {
    "test": "bun test",
    "test:py": "cd py && uv run pytest -q",
    "lint": "biome check .",
    "lint:py": "cd py && uv run ruff check .",
    "format:py": "cd py && uv run ruff format .",
    "build:graph": "cd py && uv run python -m jobshield.graph.build",
    "serve:api": "cd ts/api && bun run dev",
    "serve:web": "cd ts/web && bun run dev"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "bun-types": "1.3.14",
    "typescript": "5.6.3"
  }
}
